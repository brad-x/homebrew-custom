--- server/red-worker.c.orig	2021-03-03 21:27:54.000000000 -0500
+++ server/red-worker.c	2021-03-03 21:29:09.000000000 -0500
@@ -1120,6 +1120,11 @@
     RedWorker *worker = arg;
 
     spice_debug("begin");
+
+    #if defined(__APPLE__)
+        pthread_setname_np("SPICE Worker");
+    #endif
+
     SPICE_VERIFY(MAX_PIPE_SIZE > WIDE_CLIENT_ACK_WINDOW &&
            MAX_PIPE_SIZE > NARROW_CLIENT_ACK_WINDOW); //ensure wakeup by ack message
 
@@ -1159,8 +1164,9 @@
 #ifndef _WIN32
     pthread_sigmask(SIG_SETMASK, &curr_sig_mask, NULL);
 #endif
+#if !defined(__APPLE__)
     pthread_setname_np(worker->thread, "SPICE Worker");
-
+#endif
     return r == 0;
 }
 
